import React from 'react';

// Layout for all pages under /digital-marketing
export default function DigitalMarketingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
